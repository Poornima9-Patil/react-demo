import React from 'react'

const Paths = (props) => {
  
  
  return (
    <div className='useclass'>
      
   <h1>Here we are using Props and {props.name}</h1>
   
    </div>
  )
}

export default Paths;